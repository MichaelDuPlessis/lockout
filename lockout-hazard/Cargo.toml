[package]
name = "lockout-hazard"
version = "0.1.0"
edition = "2024"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(loom)'] }

[dependencies]

[target.'cfg(loom)'.dependencies]
loom = "0.7"
