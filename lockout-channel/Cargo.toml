[package]
name = "lockout-channel"
version = "0.1.0"
edition = "2024"

[dependencies]
lockout-hazard = { path = "../lockout-hazard" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
crossbeam-channel = "0.5"

[[bench]]
name = "mpmc_throughput"
harness = false
